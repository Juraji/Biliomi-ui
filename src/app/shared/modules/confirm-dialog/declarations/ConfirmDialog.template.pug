p {{dialogData.message}}

mat-dialog-actions
    button(mat-raised-button, color="primary", (click)="confirm()")
        | {{dialogData.confirmButtonText}}
    button(mat-raised-button, color="warn", (click)="cancel()")
        | {{dialogData.cancelButtonText}}
