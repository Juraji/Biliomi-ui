p Restart or shutdown Biliomi.
mat-dialog-actions
  button(mat-raised-button, (click)="restartBiliomi()")
    mat-icon refresh
  button(mat-raised-button, (click)="shutdownBiliomi()")
    mat-icon power_settings_new
