form(#strikesAndExcemptionSettingsForm="", (submit)="saveSettings()")
  mat-card
    mat-card-header
      mat-card-title Strikes and Group Excemption

    mat-card-content
      p.
        Messages by users in the excempted group (or higher) are not evaluated by the chat moderator.#[br]
        For example if you set the excempted group to Viewer all users will be excempted from moderation.
      user-group-selector([(userGroup)]="exemptedGroup")
      //user-group-select(#exemptedGroupControl="", placeholder="Excempted User Group")

      hr

      p.
        Biliomi keeps moderation strikes per user for the duration of an hour.#[br]
        The first moderation action will perform strike 1, the second Strike 2 and 3 and beyond will execute Strike 3.
      strike-selector([(strike)]="firstStrike")
      strike-selector([(strike)]="secondStrike")
      strike-selector([(strike)]="thirdStrike")

    mat-card-actions
      save-button([stateEmitter]="_chatModeratorSettingsClient.afterSave")
      button(mat-button, type="button", (click)="ngAfterViewInit()", matTooltip="Refresh data")
        mat-icon refresh
